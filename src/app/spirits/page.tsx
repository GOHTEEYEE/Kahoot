import { SpiritsClient } from "../../components/SpiritsClient";

export default function SpiritsPage() {
  return (
    <main className="flex min-h-full flex-1 flex-col">
      <SpiritsClient />
    </main>
  );
}
